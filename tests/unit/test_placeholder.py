"""S0.1 placeholder test -- verify test runner works (no production code yet)."""


def test_placeholder() -> None:
    """Empty test that always passes -- required to keep `make test` green in S0.1."""
    assert True
